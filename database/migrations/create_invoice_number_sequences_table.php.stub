<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('invoice_number_sequences', function (Blueprint $table) {
            $table->id();
            $table->enum('invoice_type', ['quotation', 'tax_invoice', 'credit_note', 'debit_note', 'receipt_voucher']);
            $table->string('financial_year', 10);
            $table->string('prefix', 10);
            $table->unsignedBigInteger('last_number')->default(0);
            $table->timestamps();

            $table->unique(['invoice_type', 'financial_year'], 'inv_num_seq_type_fy_unique');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('invoice_number_sequences');
    }
};
